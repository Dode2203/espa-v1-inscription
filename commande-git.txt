#Pour commiter 
git add .
git commit -m "variable et donnee"



#pour envoyer sur le main 
git push -u origin Dode_back

#Pour creer une branche
git branch Dode_back

#Pour changer de branche
git checkout Dode_back

git checkout main

git config --global user.name "Dode2203"
git config --global user.email "randriadode@gmail.com"
